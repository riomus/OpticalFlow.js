/* OpticalFlow.js main */

// Base function.
var OpticalFlow = function() {
  // Add functionality here.
  return true;
};


// Version.
OpticalFlow.VERSION = '0.0.0';


// Export to the root, which is probably `window`.
root.OpticalFlow = OpticalFlow;
